<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/uploadFragment">
    <fragment android:id="@+id/uploadFragment" android:name="com.aiquizgenerator.ui.upload.UploadFragment" android:label="Upload">
        <action android:id="@+id/action_upload_to_config" app:destination="@id/configFragment"/>
    </fragment>
    <fragment android:id="@+id/configFragment" android:name="com.aiquizgenerator.ui.config.ConfigFragment" android:label="Setup">
        <action android:id="@+id/action_config_to_quiz" app:destination="@id/quizFragment" app:popUpTo="@id/configFragment" app:popUpToInclusive="false"/>
    </fragment>
    <fragment android:id="@+id/quizFragment" android:name="com.aiquizgenerator.ui.quiz.QuizFragment" android:label="Quiz">
        <action android:id="@+id/action_quiz_to_results" app:destination="@id/resultsFragment" app:popUpTo="@id/quizFragment" app:popUpToInclusive="true"/>
    </fragment>
    <fragment android:id="@+id/resultsFragment" android:name="com.aiquizgenerator.ui.results.ResultsFragment" android:label="Results"/>
</navigation>